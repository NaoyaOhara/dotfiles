[user]
  name = Ohara Naoya
  email = ohara_naoya@kakaku.com
[push]
  default = current
[alias]
  gl = log --oneline --graph --decorate --color
  st = status
  ch = checkout
	delete-merged-branches = !git branch --merged | grep -v \\* | xargs -I % git branch -d %
